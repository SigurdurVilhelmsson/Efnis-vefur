{
  "name": "efnis-vefur",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "description": "Vefur Efnafræðifélags Íslands (efn.is) — Astro + Sveltia CMS",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "check:cms": "node --experimental-strip-types --no-warnings scripts/check-cms-config.mjs",
    "build": "npm run check:cms && astro check && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/markdown-satteri": "^0.4.2",
    "astro": "^7.3.5"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3",
    "yaml": "^2.9.1"
  }
}
